<p>Filter by Organizer</p>

<%= form_tag(admin_bookings_path, method: 'get') do %>

  <%= select_tag :query, options_from_collection_for_select(current_user.organizers, :id, :name), include_blank: true %>
  
  <%= submit_tag "Filter", class: 'btn' %>

<% end %>